# platform-observability

Rust-first OMNIXIUS service with a minimal Axum HTTP server.

## Endpoints

- GET /health -> ok

## Commands

- cargo run
- cargo test
- cargo fmt --all
- cargo clippy --all-targets --all-features -- -D warnings
